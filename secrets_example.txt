# Example secrets file - rename to secrets.cpp and move to src directory

#include "secrets.h"

// WiFi Credentials
const char* ssid = "Smart_Poi8";
const char* password = "SmartOne";

// Debug Mode
const bool debug_mode = true;  // Set to false for production

// Rotation Axis (0=X, 1=Y, 2=Z)
const int rotation_axis = 0;
